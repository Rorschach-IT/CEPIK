<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CepikAppWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CepikAppWinUI.UserControlls"
    xmlns:controls="using:CepikAppWinUI.UserControlls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    Title="Centralna Ewidencja pojazdów i kierowców">

    <Grid Padding="20"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          Background="{StaticResource LightGreenRadialBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45"/>
            <ColumnDefinition Width="0.9*"/>
            <ColumnDefinition Width="45"/>
            <ColumnDefinition Width="45"/>
            <ColumnDefinition Width="45"/>
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid Background="Transparent">
                                <TextBlock x:Name="ContentText"
                                   Text="{TemplateBinding Content}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="14"/>

                                <Border x:Name="Underline"
                                Height="1"
                                Background="{TemplateBinding Foreground}"
                                VerticalAlignment="Bottom"
                                Margin="0,0,0,2"
                                Opacity="0"/>

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="Underline"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.1"/>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <FontIcon Grid.Row="0" Grid.Column="0" Glyph="&#xE80F;" FontSize="25"/>

        <StackPanel Grid.Row="0" 
                    Grid.Column="1"
                    Grid.ColumnSpan="3"
                    Orientation="Horizontal" 
                    Padding="20"
                    HorizontalAlignment="Center"
                    Spacing="15">
            <Button Content="Pojazdy" 
                         Margin="5" 
                         Foreground="#029c3d"
                         Click="ShowVehicles"/>
            <Button Content="Osoby" 
                         Margin="5" 
                         Foreground="black"
                         Click="ShowPeople"/>
            <Button Content="Przypisania pojazdów" 
                         Margin="5" 
                         Foreground="black"
                         Click="ShowVehicleOwners"/>
            <Button Content="Przypisania ubezpieczeń" 
                         Margin="5" 
                         Foreground="black"
                         Click="ShowInsurances"/>
            <Button Content="Uprawnienia" 
                         Margin="5" 
                         Foreground="black"
                         Click="ShowPermissions"/>
            <Button Content="Kategorie prawa jazdy" 
                         Margin="5" 
                         Foreground="black"
                         Click="ShowLicenceCategories"/>
            <Button Content="Zdarzenia pojazdu" 
                         Margin="5" 
                         Foreground="black"
                         Click="ShowVehiclesEvents"/>
            <Button Content="Wykroczenia" 
                         Margin="5" 
                         Foreground="red"
                         Click="ShowOffences"/>
        </StackPanel>

        <FontIcon Grid.Row="0" Grid.Column="4" Glyph="&#xE713;" FontSize="25"/>

        <Grid x:Name="MainContentArea"
              Background="Transparent"
              Grid.Row="2" 
              Grid.Column="0" 
              Grid.ColumnSpan="5"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"/>
    </Grid>
</Window>
    